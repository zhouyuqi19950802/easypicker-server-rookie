# EasyPicker2-Server

EasyPicker（轻取）服务端 - 为提高在线文件收取效率而生

## 📋 功能特点

### 核心功能
- **文件收取管理** - 支持创建文件收取任务，批量收取用户提交的文件
- **用户管理** - 完整的用户系统，支持用户注册、登录、权限管理
- **任务管理** - 创建、编辑、删除文件收取任务，支持任务分类
- **文件处理** - 支持文件上传、预览、下载、打包下载
- **数据统计** - 提供详细的文件收取统计和数据分析

### 技术特性
- **多数据库支持** - 支持 MySQL、MongoDB、Redis 数据存储
- **本地集成** - 改七牛云为本地存储
- **图片处理** - 使用 Sharp 进行图片压缩和处理
- **邮件服务** - 集成 Nodemailer 支持邮件通知
- **TypeScript** - 完全使用 TypeScript 开发，类型安全
- **RESTful API** - 提供标准的 RESTful 接口

## 🚀 快速开始

### 环境要求
- Node.js >= 14.0.0
- MySQL >= 5.7
- MongoDB >= 4.0
- Redis >= 5.0
- pnpm >= 6.0.0

### 安装依赖
```bash
# 使用 pnpm 安装依赖
pnpm install
```

### 环境配置

项目支持自动配置，首次启动时会：

1. **自动生成配置文件** - 创建 `user-config.json` 和 `.env.local`
2. **自动创建管理员账号** - 生成随机的管理员账号和密码
3. **自动初始化数据库配置** - 使用默认的数据库连接配置
4. **自动创建数据表** - 首次连接数据库时自动创建所需表结构

#### 默认配置
- **MySQL**: 127.0.0.1:3306，数据库名 `ep-dev`
- **MongoDB**: 127.0.0.1:27017，数据库名 `ep-dev`  
- **Redis**: 127.0.0.1:6379
- **服务端口**: 3000

> 💡 **提示**: 如果需要修改配置，可以通过管理面板或直接编辑 `user-config.json` 文件

#### 手动配置（可选）
如需自定义配置，可以创建 `.env.local` 文件：

```env
# 覆盖默认配置
SERVER_PORT=8080
MYSQL_DB_HOST=your_mysql_host
# ... 其他配置
```

### 数据库初始化

确保 MySQL、MongoDB、Redis 服务已启动，项目会在首次启动时：

1. **自动创建数据表** - 在 MySQL 中创建所需的表结构
2. **自动添加字段** - 检查并添加新增的字段
3. **自动生成配置** - 创建默认的系统配置
4. **生成管理员账号** - 在控制台输出管理面板的账号密码

> ⚠️ **重要**: 首次启动时请留意控制台输出的管理员账号信息，用于访问管理面板

### 启动方式

#### 开发模式
```bash
# 启动开发环境（自动监听文件变化并重启）
pnpm dev
```

#### 生产模式
```bash
# 构建项目
pnpm build

# 启动生产环境
pnpm start
```

#### 直接运行 TypeScript
```bash
# 直接运行 TypeScript 文件（适合调试）
pnpm start:ts
```

> 🚀 **快速启动**: 只需运行 `pnpm dev`，项目会自动完成所有初始化配置！

## 📁 项目结构

```
src/
├── config/          # 配置文件
├── constants/       # 常量定义
├── controllers/     # 控制器
├── db/             # 数据库相关
├── middleware/     # 中间件
├── routes/         # 路由定义
├── utils/          # 工具函数
└── index.ts        # 入口文件
```

## 🔧 开发工具

```bash
# 代码检查
pnpm lint

# 自动修复代码格式
pnpm lint:fix

# 运行测试
pnpm test

# 构建项目
pnpm build
```

## 📚 相关文档

- [API 文档](https://easy2.w.eolink.com/share/index?shareCode=7SF9Na)
- [部署指南](https://docs.ep.sugarat.top/)
- [数据库设计](./docs)
- [更新日志](https://docs.ep.sugarat.top/plan/log.html)

## 🌐 在线体验

- **正式环境**: https://ep.sugarat.top
- **测试环境**: https://ep.test.sugarat.top

## 🤝 贡献

欢迎提交 Issue 和 Pull Request 来完善项目。

## 📄 许可证

MIT License

## 🙏 致谢

感谢所有为 EasyPicker 项目做出贡献的开发者。
